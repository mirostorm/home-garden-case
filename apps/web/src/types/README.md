# src/types

This folder holds all the application types used, typically a file is created per concept.
For example if our concept was `@labels` we would have a matching `label.types.ts` file in this folder.

## Naming convention

```
<singular concept name>.types.ts
```

Examples: `user.types.ts`, `garden.types.ts`
